<div id="layoutAuthentication">
	<div id="layoutAuthentication_content">
		<main>
			<div class="container">
				<div class="row justify-content-center">
					<div class="col-lg-10">
						<div class="card shadow-lg border-0 rounded-lg mt-5">
							<div class="card-header"><h3 class="text-center font-weight-light my-4">Edit Folder</h3>
								<div class="card-body">

									<%= render 'form', folder: @folder %>

									<%= link_to 'Show', @folder %> |
									<%= link_to "Back", {controller: "patients", action: "show", id: Folder.find_by_id(params[:id]).patient_id} %>
							</div>
						</div>
					</div>
				</div>
			</div>
		</main>
	</div>
</div>
